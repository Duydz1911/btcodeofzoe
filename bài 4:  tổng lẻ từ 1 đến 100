#include <stdio.h>
int main() {
    int sum = 0; // tổng gốc bằng 0
    for (int i = 1; i <= 100; i += 2) {  // số lẻ
       sum += i; // lẻ cộng vào tổng .....
    }
    printf("tổng các số lẻ từ 1 đến 100 là : %d", sum );
    return 0;
}
